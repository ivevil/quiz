/* eslint-disable */

const Indicator = ({ children }) => {

    return (
        <span className="total-question flex text-white">
            {children}
        </span>
    );
};

export default Indicator;